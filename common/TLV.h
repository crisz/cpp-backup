#include <boost/any.hpp>
#include <map>

class TLV {
    char* encode(boost::any plain) {
        std::string str;
        str.append()
    }

    std::map<std::string, boost::any> decode(char* encoded) {

    }
};